<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.niuchaoqun.springboot.mapper.AdminMapper">
    <resultMap id="AdminRelationResultMap" type="Admin">
        <id property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="username" column="username"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="created" column="created"/>
        <result property="updated" column="updated"/>
        <result property="createdby" column="createdby"/>
        <result property="updatedby" column="updatedby"/>
        <result property="state" column="state"/>
        <result property="lastLoginIp" column="last_login_ip"/>
        <result property="lastLogined" column="last_logined"/>
        <association property="role" javaType="Role">
            <id property="id" column="r_id"/>
            <result property="role" column="r_role"/>
            <result property="name" column="r_name"/>
        </association>
    </resultMap>

    <select id="getRelationById" parameterType="long" resultMap="AdminRelationResultMap">
        SELECT a.*, r.id as r_id, r.name as r_name, r.role as r_role
        FROM admin a, role r
        WHERE a.id = #{id} AND a.role_id = r.id
    </select>

    <select id="getRelationByUsername" parameterType="string" resultMap="AdminRelationResultMap">
        SELECT a.*, r.id as r_id, r.name as r_name, r.role as r_role
        FROM admin a, role r
        WHERE a.username = #{username} AND a.role_id = r.id
    </select>

</mapper>